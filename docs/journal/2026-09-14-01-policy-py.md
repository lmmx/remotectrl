# 2026-09-14: Unit — `policy.py`

**Attempting:** `src/remotectrl/policy.py::ok(ahead, behind, mine)`
**Derived from:** `docs/journal/2026-09-14-gitwrap-first-slice.md` §1 (kernel predicate,
itself derived from sumac `docs/journal/2026-09-13-remotectrl-design.md` §2, §5)
**Checkpoint before starting:** `/tmp/remotectrl-checkpoints/20260914T004955-before-policy-py`

Predicate: `ok(ahead, behind, mine) = (behind == 0) if mine else (ahead == 0)`. Pure function,
no I/O, no `Path` argument — takes only the two integers `ahead_behind` will produce and the
`mine` flag callers derive from `current_branch() == branch_under_check`.

**Outcome:** done. `src/remotectrl/policy.py` (9 lines), `tests/test_policy.py` — full
10-case truth table over `{ahead∈{0,5,100}, behind∈{0,1,5,100}, mine∈{True,False}}` including
the zero-zero case for both `mine` values. All pass under Python 3.14 (`/tmp/remotectrl-venv`).
Sibling-instance grep: none applicable yet — no other module exists to duplicate this logic.
