# 2026-09-14: Unit — `gitwrap.fetch_all`, `gitwrap.push_branch`

**Attempting:** `src/remotectrl/gitwrap.py::fetch_all(path, remote)`,
`push_branch(path, remote, branch)`
**Derived from:** sumac `docs/journal/2026-09-13-remotectrl-design.md` §5 and §7, as amended
2026-09-14 — generic refspec `refs/heads/*:refs/remotes/<remote>/*` for fetch, and
`HEAD:refs/heads/<branch>` for push, with no branch-naming assumption in either.
**Checkpoint before starting:** `/tmp/remotectrl-checkpoints/20260914T005805-before-fetch-push`

These are the two functions that mutate state (remote-tracking refs, and the actual remote),
so higher risk than the read-only trio — each gets a test against a real second git repo
acting as the "remote" (a plain local repo added via `git remote add`, no network involved,
no sumac repo touched, per session rule 6).

**Outcome:** done, with one test bug caught and fixed mid-unit: an assertion comparing
`head_commit(remote) != head_commit(git_repo)` failed because both fixture repos legitimately
share the same seed-commit hash (identical content/message/author) — not a `gitwrap` bug,
a bad test assumption. Removed the assertion; the branch-existence checks that follow it
already cover what the test intends to verify. 4/4 tests pass after the fix; full suite 25/25.

Sibling-instance grep: `refs/heads/*` and `refs/heads/<branch>` string construction exists
in exactly these two call sites (`fetch_all`, `push_branch`) — no hardcoded branch name or
naming-convention pattern anywhere in `src/`.
